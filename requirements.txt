# ============================================================================
# WEB FRAMEWORK & API
# ============================================================================
fastapi==0.116.1          # Modern, fast web framework for building APIs with Python
uvicorn[standard]==0.35.0  # ASGI server for running FastAPI applications

# ============================================================================
# DATA VALIDATION & SETTINGS
# ============================================================================
pydantic==2.11.7          # Data validation using Python type annotations
pydantic-settings==2.10.1 # Settings management with Pydantic (env vars, .env files)

# ============================================================================
# DEPENDENCY INJECTION
# ============================================================================
dependency-injector==4.48.1  # Dependency injection framework for Clean Architecture

# ============================================================================
# HTTP CLIENT & NETWORKING
# ============================================================================
httpx==0.28.1  # Async HTTP client for FIWARE integrations (STH-Comet, IoT Agent)

# ============================================================================
# TASK QUEUE & MESSAGE BROKER
# ============================================================================
celery==5.5.3     # Distributed task queue for asynchronous ML training
redis==6.4.0      # In-memory data store (Celery broker/backend)
types-redis==4.6.0.20241004  # Type hints for Redis client
pika==1.3.2       # Pure Python RabbitMQ/AMQP client library

# ============================================================================
# DATABASE
# ============================================================================
pymongo==4.14.1  # MongoDB driver for model configs and training job persistence

# ============================================================================
# MACHINE LEARNING & DATA SCIENCE
# ============================================================================
# Deep Learning Framework
tensorflow==2.20.0      # Main TensorFlow package for LSTM/GRU models
tensorflow-cpu==2.20.0  # CPU-optimized TensorFlow (fallback for non-GPU environments)

# Data Processing & Classical ML
scikit-learn==1.7.2  # ML utilities (preprocessing, metrics, train/test splits)
pandas==2.3.2        # Data manipulation and analysis (time series handling)
numpy==2.3.3         # Numerical computing foundation for all ML operations

# Model Persistence
joblib==1.5.2  # Efficient serialization for scikit-learn objects (scalers, preprocessors)

# ============================================================================
# LOGGING
# ============================================================================
structlog==25.4.0  # Structured logging for better observability and debugging

# ============================================================================
# DEVELOPMENT & CODE QUALITY
# ============================================================================
pre-commit==4.3.0  # Git hooks framework for code quality automation
black==25.1.0      # Code formatter for consistent Python style
isort==6.0.1       # Import statement organizer
flake8==7.3.0      # Linting tool for code quality checks
mypy==1.17.1       # Static type checker for Python
